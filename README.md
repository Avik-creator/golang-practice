# golang-practice

Interview-style Go exercises.

- `finiteconcurrentwebcrawler` — bounded concurrent crawler (visited set, semaphore, WaitGroup)
- `work_queue` — in-memory job queue with FIFO, retries, and a dead-letter queue
